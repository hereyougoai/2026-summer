## 線段樹練習：光表時間軸的區間染色

編譯：g++ -std=c++17 -Wall segtree_test.cpp -o segtree_test
執行：./segtree_test

你只需要修改「實作區」裡的三個函式。
測試區的程式碼請不要更動。

## 題目定義

給定一條長度為 `n` 的時間軸，格子編號 `0` 到 `n-1`，初始值全部為 `0`（代表熄滅）。 支援兩種操作：


| 操作              | 意義                                |
| ----------------- | ----------------------------------- |
| `assign(l, r, c)` | 把區間`[l, r)`內所有格子的值設成`c` |
| `query(pos)`      | 回傳第`pos`格目前的值               |

區間一律是**左閉右開**：`assign(2, 5, 7)` 影響的是第 2、3、4 格，不包含第 5 格。

顏色以 `int` 表示。實務上那是打包後的 32 位元 RGBA，但本題只把它當成一個不透明的整數， 不需要理解它的內部結構。

**目標複雜度**：兩種操作都要做到 `O(log n)`。
